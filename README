`import.py` Used to import my hexo note to obsidian

`replace.py` 
Used to replace the {filename}/pic to ../InsertPic
Warning !!!!
Bad script
I should just use to filename to replace instead of the stupid regex